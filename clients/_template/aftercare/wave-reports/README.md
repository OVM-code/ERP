# Wave-impact reports

One report per Business Central release wave (April/October), generated with the
`/wave-impact` skill: the Microsoft release plan + add-on notes, filtered to THIS
client's in-scope scenarios and stack, classified as `let-op` / `test` / `kans` /
`terminologie`, with owners and actions. File name: `2026-wave-2.md`.

The `kans` section is the commercial heart: a new standard feature that can replace
a GAP is a change request the client *wants* to receive.
